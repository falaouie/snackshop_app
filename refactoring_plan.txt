1 SearchWidget

Replace hardcoded width/height with layout config
Move inline styles to styles/components.py


2 numpadWidget

Replace button size and display height hardcoding
Move button styles to styles/components.py


3 USDPresetWidget

Replace fixed button sizes with layout config
Move inline button styles to styles file


4 LBPPresetWidget

Replace fixed button sizes with layout config
Move inline button styles to styles file


5 CardPaymentWidget & PaymentOptionWidget

Replace any hardcoded dimensions
Move styles to styles/payment_widgets.py


6 OtherPaymentWidget

Align with CardPaymentWidget changes
Ensure consistent styling


7 TransactionButtonsWidget

Replace fixed button dimensions
Move frame styles to appropriate style file


8 ProductGridWidget

Replace fixed grid dimensions
Centralize product button styling


9 OrderListWidget

Replace fixed dimensions for labels and containers
Move inline styles to style files


10 KeyboardWidget

Replace hardcoded key dimensions
Maintain complex interaction styling